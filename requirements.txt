# Core dependencies (all platforms)
python-dateutil>=2.8.0
reportlab>=4.0.0
pypdf>=4.0.0

# For local Excel editing (optional):
# openpyxl>=3.1.0

# Windows-only (for generate_bills.py and run_bills_console.py):
# pip install pywin32
